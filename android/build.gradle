plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.14'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'starslookup-api'
            version = '1.1.14'

            from components.java

            pom {
                name = 'Stars Lookup'
                description = 'Stars Lookup is a simple tool for looking up star data. It returns the star name, mass, diameter, and more.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/starslookup-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/starslookup-api.git'
                    url = 'https://github.com/apiverve/starslookup-api'
                }
            }
        }
    }
}
